{
  "name": "investreal",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "test": "npm run test --workspace @investreal/engine",
    "build": "npm run build --workspace @investreal/engine"
  }
}
